---
phase: 05-access-control-cache-ttl
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/middleware/access.ts
  - src/middleware/access.test.ts
autonomous: true
requirements:
  - ACL-01
  - ACL-02
  - ACL-03
  - ACL-04
  - ACL-05

must_haves:
  truths:
    - "checkAccess with public+payments mode, whitelisted pubkey, action=upload returns allowed:true"
    - "checkAccess with public+payments mode, blacklisted pubkey, action=upload returns allowed:false with reason 'blacklisted' (not requiresPayment)"
    - "checkAccess with public+payments mode, unlisted pubkey, action=upload returns allowed:false with requiresPayment:true and reason 'payment_required'"
    - "checkAccess with public+payments mode, unlisted pubkey, action=delete returns allowed:true (delete always free)"
    - "checkAccess with plain public mode behaves identically to v1 (no change)"
    - "checkAccess with private mode behaves identically to v1 (no change)"
    - "normalizeAccessConfig with payments=true, public=false forces payments=false and logs warning"
    - "normalizeAccessConfig with missing payments field defaults to false silently (no log)"
  artifacts:
    - path: "src/types.ts"
      provides: "AccessConfig with payments field"
      contains: "payments"
    - path: "src/middleware/access.ts"
      provides: "checkAccess with action parameter, AccessAction type, public+payments branch"
      exports: ["checkAccess", "AccessResult", "AccessAction", "loadAccessConfig", "_resetAccessCacheForTesting"]
    - path: "src/middleware/access.test.ts"
      provides: "Full ACL test suite covering public, private, and public+payments modes"
      min_lines: 80
  key_links:
    - from: "src/middleware/access.ts"
      to: "src/types.ts"
      via: "AccessConfig import"
      pattern: "import.*AccessConfig.*from.*types"
    - from: "src/middleware/access.ts"
      to: "normalizeAccessConfig"
      via: "payments field normalization"
      pattern: "payments.*boolean.*false"
---

<objective>
Extend checkAccess() with public+payments mode and the action parameter, refactor test suite to cover all three operating modes systematically.

Purpose: The access control layer must correctly route unlisted pubkeys to payment in public+payments mode, with blacklist always taking priority, while keeping existing modes unchanged.
Output: Updated access.ts with AccessAction type, three-mode checkAccess(), updated normalizer; refactored access.test.ts covering all modes.
</objective>

<execution_context>
@/home/sandwich/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandwich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/types.ts
@src/middleware/access.ts
@src/middleware/access.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add payments field to AccessConfig and write failing tests for public+payments mode</name>
  <files>src/types.ts, src/middleware/access.test.ts</files>
  <action>
1. In `src/types.ts`, add `payments?: boolean` field to AccessConfig interface with JSDoc:
   ```typescript
   /** true = public+payments mode (unlisted pubkeys routed to payment). Only meaningful when public=true. Default: false */
   payments?: boolean;
   ```

2. In `src/middleware/access.test.ts`, REPLACE the entire test suite with a systematic refactored suite covering all three modes. Add a new pubkey constant `PUB_UNLISTED = "e".repeat(64)` for clarity.

   Import `AccessAction` type from access.ts (will not exist yet — that's the RED step). Update all existing `checkAccess(storage, pubkey)` calls to `checkAccess(storage, pubkey, "upload")` since the signature is changing.

   **Public mode tests (preserve existing behavior, updated signature):**
   - ACL-05 backward compat: public mode, clean pubkey, action=upload → allowed:true
   - ACL-05 backward compat: public mode, blacklisted pubkey, action=upload → denied, reason contains "blacklisted"
   - ACL-05 backward compat: public mode, whitelisted pubkey, action=upload → allowed (whitelist is no-op in plain public)
   - ACL-05 backward compat: public mode, pubkey on both lists, action=upload → denied (blacklist wins)

   **Private mode tests (preserve existing behavior, updated signature):**
   - ACL-05 backward compat: private mode, whitelisted pubkey, action=upload → allowed
   - ACL-05 backward compat: private mode, clean pubkey, action=upload → denied, user-facing reason
   - ACL-05 backward compat: private mode, blacklisted-only pubkey, action=upload → denied

   **Public+payments mode tests (NEW):**
   - ACL-01: public+payments, config has payments:true → mode is active (config normalizes correctly)
   - ACL-02: public+payments, whitelisted pubkey, action=upload → allowed:true (no payment required)
   - ACL-03: public+payments, blacklisted pubkey, action=upload → denied with reason "blacklisted" (NOT requiresPayment)
   - ACL-04: public+payments, unlisted pubkey, action=upload → denied with requiresPayment:true, reason="payment_required"
   - ACL-04: public+payments, unlisted pubkey, action=mirror → denied with requiresPayment:true (mirror also gated)
   - ACL-04: public+payments, unlisted pubkey, action=delete → allowed:true (delete always free)
   - ACL-03 edge: public+payments, pubkey on both lists, action=upload → denied "blacklisted" (blacklist wins over whitelist)

   **Normalizer tests (NEW):**
   - payments field missing → defaults to false (no console.warn)
   - payments=true, public=true → payments=true
   - payments=true, public=false → payments=false (console.warn fires)
   - payments="string" → defaults to false
   - payments=null → defaults to false

3. Run tests — they MUST FAIL because AccessAction type and action parameter don't exist yet.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/blssm.us && deno test src/middleware/access.test.ts 2>&1 | tail -20</automated>
    <manual>Tests should fail with compilation/type errors (AccessAction not found, checkAccess expects 2 args got 3)</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Tests exist for all three modes (public, private, public+payments) and for normalizer edge cases. Tests fail because implementation doesn't exist yet.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AccessAction, update normalizer and checkAccess to pass all tests</name>
  <files>src/middleware/access.ts</files>
  <action>
1. In `src/middleware/access.ts`, add and export `AccessAction` type:
   ```typescript
   export type AccessAction = "upload" | "mirror" | "delete";
   ```

2. Update `normalizeAccessConfig()` to handle `payments` field:
   - Extract `payments` as boolean, default false if missing or non-boolean
   - If `payments=true` and `public=false`: `console.warn("[access] payments=true ignored in private mode (public=false)")` and force `payments: false`
   - If `payments` field is absent: do NOT log anything (silent upgrade per CONTEXT.md)
   - Include `payments` in returned AccessConfig object

3. Update `DEFAULT_ACCESS_CONFIG` to include `payments: false`.

4. Update `checkAccess()` signature to accept action parameter:
   ```typescript
   export async function checkAccess(
     storage: StorageClient,
     pubkey: string,
     action: AccessAction,
   ): Promise<AccessResult>
   ```

5. Update the decision matrix inside `checkAccess()`:
   - Public mode: blacklist check first (unchanged). Then:
     - If `config.payments && action !== "delete"`: check whitelist → if whitelisted, return allowed. Otherwise return `{ allowed: false, reason: "payment_required", requiresPayment: true }`.
     - If not payments mode (or action is delete): return allowed (unchanged behavior).
   - Private mode: unchanged (whitelist-only path).

6. Update the JSDoc decision matrix comment to include the new v1.1 rows:
   ```
   *   pub+pay  | blacklisted         | DENY 403  | ACL-03
   *   pub+pay  | whitelisted         | ALLOW     | ACL-02
   *   pub+pay  | unlisted + upload    | PAY 402   | ACL-04
   *   pub+pay  | unlisted + mirror    | PAY 402   | ACL-04
   *   pub+pay  | unlisted + delete    | ALLOW     | ACL-04 (delete always free)
   ```

7. Run tests — they MUST ALL PASS.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/blssm.us && deno test src/middleware/access.test.ts</automated>
    <manual>All tests pass with 0 failures</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>checkAccess() correctly handles public, private, and public+payments modes. All test cases pass. AccessAction type is exported. Normalizer enforces private+payments invariant.</done>
</task>

</tasks>

<verification>
1. `deno test src/middleware/access.test.ts` — all tests pass, 0 failures
2. `deno check src/middleware/access.ts` — no type errors
3. Test count: minimum 18 tests (8 existing refactored + 7 public+payments + 5 normalizer)
</verification>

<success_criteria>
- All ACL requirement test cases pass
- checkAccess() signature includes action parameter
- AccessAction type exported from access.ts
- normalizeAccessConfig handles payments field with silent default and private-mode warning
- Existing public and private mode behavior unchanged (ACL-05)
- No new external dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/05-access-control-cache-ttl/05-01-SUMMARY.md`
</output>
