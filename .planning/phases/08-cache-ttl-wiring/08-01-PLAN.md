---
phase: 08-cache-ttl-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/middleware/payment-gate.ts
autonomous: true
requirements:
  - CACHE-01
  - CACHE-02
gap_closure: true

must_haves:
  truths:
    - "loadCacheConfig(storage) is called in production code at runtime"
    - "checkAccess() receives accessTtl from CacheConfig (not the hardcoded 60s constant)"
    - "loadPaymentConfig() receives paymentTtl from CacheConfig (not the hardcoded 60s constant)"
    - "isBlocked() receives blockedTtl from CacheConfig (not the hardcoded 60s constant)"
    - "Changing accessTtl, paymentTtl, or blockedTtl in config/cache.json changes actual cache duration"
  artifacts:
    - path: "src/middleware/payment-gate.ts"
      provides: "paymentGate() calls loadCacheConfig and threads TTL values to all three caches"
      contains: "loadCacheConfig"
  key_links:
    - from: "src/middleware/cache-config.ts"
      to: "src/middleware/payment-gate.ts"
      via: "import loadCacheConfig"
      pattern: "loadCacheConfig"
    - from: "src/middleware/payment-gate.ts"
      to: "src/middleware/access.ts checkAccess"
      via: "cacheConfig.accessTtl passed as ttlMs"
      pattern: "accessTtl"
    - from: "src/middleware/payment-gate.ts"
      to: "src/middleware/payment-config.ts loadPaymentConfig"
      via: "cacheConfig.paymentTtl passed as ttlMs"
      pattern: "paymentTtl"
    - from: "src/middleware/payment-gate.ts"
      to: "src/storage/metadata.ts isBlocked"
      via: "cacheConfig.blockedTtl passed as ttlMs"
      pattern: "blockedTtl"
---

<objective>
Wire loadCacheConfig(storage) into paymentGate() so operator-configured TTL values from config/cache.json are applied at runtime to all three cache modules.

Purpose: loadCacheConfig() is exported and tested (12 tests pass) but has zero production imports. checkAccess(), loadPaymentConfig(), and isBlocked() all accept optional ttlMs parameters (added in Phase 5) but no caller passes values — all three caches run on hardcoded 60-second module constants regardless of operator config. This ~10-line fix closes CACHE-01 and CACHE-02.

Output: paymentGate() calls loadCacheConfig(storage) once per request, then passes the returned CacheConfig values (accessTtl, paymentTtl, blockedTtl) into the existing ttlMs parameters of all three downstream calls.
</objective>

<execution_context>
@/home/sandwich/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandwich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/middleware/payment-gate.ts
@src/middleware/cache-config.ts
@src/middleware/access.ts
@src/middleware/payment-config.ts
@src/storage/metadata.ts
@src/middleware/payment-gate.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire loadCacheConfig into paymentGate and thread TTL values to all three caches</name>
  <files>src/middleware/payment-gate.ts</files>
  <action>
In `src/middleware/payment-gate.ts`:

1. Add import at top of file (after existing imports):
   ```ts
   import { loadCacheConfig } from "./cache-config.ts";
   ```

2. Inside the `paymentGate()` function body, immediately after the storage parameter is available and before any cache-using calls, add:
   ```ts
   const cacheConfig = await loadCacheConfig(storage);
   ```

3. Pass TTL values to the three existing call sites that accept ttlMs:
   - `checkAccess(storage, pubkey, action)` → `checkAccess(storage, pubkey, action, cacheConfig.accessTtl)`
   - `loadPaymentConfig(storage)` → `loadPaymentConfig(storage, cacheConfig.paymentTtl)`
   - `isBlocked(storage, hash)` → `isBlocked(storage, hash, cacheConfig.blockedTtl)`

Verify the CacheConfig type fields by reading `src/middleware/cache-config.ts` — the returned object has `accessTtl`, `paymentTtl`, and `blockedTtl` number fields.

Verify the ttlMs parameter positions by reading each function signature:
- `checkAccess(storage, pubkey, action, ttlMs?)` — 4th param
- `loadPaymentConfig(storage, ttlMs?)` — 2nd param
- `isBlocked(storage, hash, ttlMs?)` — 3rd param

Do NOT change any existing logic, error handling, or response construction. This is purely threading existing values through existing parameters.

Do NOT call loadCacheConfig() in handler files (blob-upload.ts, mirror.ts, etc.) — paymentGate() is already the single call site for all three caches on the write path; wiring it there satisfies all three cache requirements with minimal surface area.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/blssm.us && deno test src/middleware/payment-gate.test.ts --allow-read --allow-env 2>&1 | tail -5</automated>
    <manual>Confirm `grep -n "loadCacheConfig" src/middleware/payment-gate.ts` shows the import and call site. Confirm `grep -n "cacheConfig\." src/middleware/payment-gate.ts` shows all three TTL pass-throughs.</manual>
  </verify>
  <done>
    - loadCacheConfig is imported and called in payment-gate.ts
    - All three ttlMs parameters receive CacheConfig values (not left as defaults)
    - All existing payment-gate tests pass without modification
    - `grep -c "loadCacheConfig" src/middleware/payment-gate.ts` returns 2 (import + call)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify full test suite passes and no production imports of hardcoded TTLs regressed</name>
  <files></files>
  <action>
Run the full test suite to confirm no regressions across all middleware and storage modules.

Then confirm the wiring is complete end-to-end:
1. `grep -rn "loadCacheConfig" src/` — must show at least 2 files: cache-config.ts (definition) and payment-gate.ts (production call site)
2. `grep -n "cacheConfig\." src/middleware/payment-gate.ts` — must show accessTtl, paymentTtl, blockedTtl all referenced
3. Confirm REQUIREMENTS.md CACHE-01 and CACHE-02 can be marked `[x]` — update both to `[x]` in `.planning/REQUIREMENTS.md`
4. Update REQUIREMENTS.md traceability table: CACHE-01 and CACHE-02 rows → Status: Complete
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/blssm.us && deno test --allow-read --allow-env --allow-net 2>&1 | tail -10</automated>
    <manual>Check grep output confirms loadCacheConfig has a production import site.</manual>
  </verify>
  <done>
    - All 87+ tests pass
    - loadCacheConfig appears in production code (not just tests)
    - REQUIREMENTS.md updated: CACHE-01 [x], CACHE-02 [x], both rows Status: Complete
    - Zero new test failures introduced
  </done>
</task>

</tasks>

<verification>
End-to-end check:
1. `grep -rn "loadCacheConfig" src/ --include="*.ts" | grep -v test` — must include payment-gate.ts
2. `deno test --allow-read --allow-env --allow-net` — all tests pass
3. REQUIREMENTS.md CACHE-01 and CACHE-02 both marked `[x]`
</verification>

<success_criteria>
- loadCacheConfig(storage) is called in production code (payment-gate.ts)
- All three ttlMs parameters receive operator-configured values: accessTtl → checkAccess, paymentTtl → loadPaymentConfig, blockedTtl → isBlocked
- Full test suite (87+ tests) passes without modification to existing tests
- CACHE-01 and CACHE-02 marked complete in REQUIREMENTS.md
</success_criteria>

<output>
After completion, create `.planning/phases/08-cache-ttl-wiring/08-01-SUMMARY.md`
</output>
